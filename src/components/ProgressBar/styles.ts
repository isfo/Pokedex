import { Dimensions, StyleSheet } from "react-native";


export const styles = StyleSheet.create({
    background:{
        marginTop: 8,
        width: '100%',
        height: 5,
        zIndex: 1
    },
    bar:{
        width: '100%',
        height: 5,
        zIndex: 10
    }
});